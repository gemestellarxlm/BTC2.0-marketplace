export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "BRC20-Marketplace",
  description: "You can buy Ordinals using BRC20 tokens.",
  navItems: [
    {
      label: "Home",
      href: "/home",
    },
  ],
};
